# BinBuddyVicPark
A small web app that tells the user when bin day is. Uses a CSV file for addresses becuase i wanted this to work out of the box without an API.

Uses nodemon (npm run dev) and has docker support, also comes with a docker image
